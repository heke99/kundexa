KUNDEXA BUILD FIX — 2026-07-27

Fel från lokal verifiering
- npm ci EUSAGE: pdf-lib krävde en separat tslib 1.14.1-post som saknades i package-lock.json.
- calls/page.tsx importerade två moduler från sökvägar som inte finns.
- api-service.ts använde typeof på variabler som TypeScript hade smalnat till null.
- api-service.ts använde sellerSnapshot i API-utskicket utan att hämta seller_snapshot från avtalet.
- pdf-lib kunde inte hittas eftersom npm ci avbröts innan beroendena installerades.

Rättningar
- package-lock.json innehåller nu node_modules/pdf-lib/node_modules/tslib 1.14.1 med korrekt tarball och integrity.
- calls/page.tsx använder befintliga @/app/actions/communications och @/components/ui/page-header.
- api-service.ts använder namngivna ApiProductRow och ApiPriceVersionRow och säker explicit typning.
- sendContractFromApi hämtar seller_snapshot och bygger juridiskt namn, branding och kontakt från den frysta snapshoten.

Verifiering i leveransmiljön
PASS: npm ci --dry-run --ignore-scripts --no-audit --no-fund --offline
      npm accepterade package.json/package-lock.json och planerade installation av både pdf-lib 1.17.1 och tslib 1.14.1.
PASS: node scripts/verify.mjs
      Verified 36 migrations, source-call-gated contracts, canonical PDFs, Resend delivery/webhooks, reminders, evidence, tenant/contact policy, licensed directory, ingestion, NIX compliance, retention and worker deployment.
PASS: node scripts/contract-delivery-unit-tests.mjs
      Stable snapshot/PDF hashes, reminder quiet hours, Resend mapping and escaped email templates.

Extern begränsning
- Ett fullständigt npm ci kunde inte slutföras i sandboxen eftersom den interna npm-gatewayen returnerade HTTP 503 för pdf-lib-tarballen.
- Kör därför npm ci, npm run typecheck, npm run build och npm run verify på den lokala Mac-miljön efter att patchen synkats.
