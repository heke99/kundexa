# Rinkel worker and team activation fix

Apply the migration, deploy the `rinkel-platform-worker` Supabase Edge Function, set the same `CRON_SECRET` in Supabase and Vercel, and deploy the Next.js application to Vercel production.

See the delivery message for exact commands.
